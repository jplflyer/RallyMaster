spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rallymaster
    username: rallymaster
    password: rallyhq
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
