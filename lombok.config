# make setters chain and return 'this'
lombok.accessors.chain = true

# (optional) keep normal getX/setX method names (not fluent)
lombok.accessors.fluent = false

# stop searching above this file
config.stopBubbling = true